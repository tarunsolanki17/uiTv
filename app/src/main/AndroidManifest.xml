<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarun.uitsocieties">

    <!-- ***************************** PERMISSIONS *********************************** -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InClub"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".Settings"
            android:label="Settings"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".EventsDetailedActivity"
            android:label="Details"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".PhotoDetailActivity"
            android:label=""
            android:theme="@style/PhotoDetailTheme">
        </activity>
        <activity android:name=".videos_fragment.VideosDetail"
            android:label=""
            android:theme="@style/VideoDetail"
            android:configChanges="orientation|keyboardHidden|screenLayout|screenSize">
        </activity>
        <activity android:name=".events_fragment.EventsDeatiledPhotoActivity"
            android:label=""
            android:theme="@style/PhotoDetailTheme"
            android:parentActivityName=".EventsDetailedActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".EventsDetailedActivity" />
        </activity>
        <activity android:name=".videos_fragment.VideosData"
            android:label="Details"
            android:theme="@style/AppTheme"
            android:parentActivityName=".videos_fragment.VideosDetail">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".videos_fragment.VideosDetail"/>
        </activity>

        <!-- *********************FACEBOOK INTEGRATION************************ -->

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- *******************JobService**************************** -->
        <service
            android:name=".FetchingJobService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.firebase.jobdispatcher.ACTION_EXECUTE" />
            </intent-filter>
        </service>
        <!-- ******************Static Broadcast Receiver********************* -->
        <receiver
            android:name=".EventsFrag$NotificationBroadCastReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>